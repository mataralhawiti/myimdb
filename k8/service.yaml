apiVersion: v1
kind: Service
metadata:
  name: myimdb
  labels:
    app: myimdb
spec:
  selector:
    app: myimdb
  type: LoadBalancer
  ports:
    - name: tcp
      protocol: TCP
      port: 5000
      targetPort: 5000
