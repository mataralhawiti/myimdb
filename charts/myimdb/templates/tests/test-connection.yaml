apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myimdb.fullname" . }}-test-connection"
  labels:
    {{- include "myimdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myimdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
